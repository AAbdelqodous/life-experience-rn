// Stub for expo/src/winter/runtime.native — avoids dynamic import() error in Jest
// This module configures the Expo "winter" runtime (WinterCG polyfills) which
// cannot run in Jest because it uses dynamic import() for native modules.
